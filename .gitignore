# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
*.local

# Environment / secrets — never commit real values
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Raw Movebank dumps (~42 MB; regenerate with `npm run fetch:data` + the curl
# commands in the README). The processed, committed dataset that the app ships
# lives at src/data/tracks.json.
scripts/raw/
